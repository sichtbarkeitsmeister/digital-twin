"use client";

import type { ReactNode } from "react";

/** Reserved for future dashboard header context. */
export function DashboardHeaderProvider({ children }: { children: ReactNode }) {
  return children;
}
